<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp">

    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:stretchColumns="*">

        <TableRow>
            <TextView android:text="Name" android:textStyle="bold"/>
            <TextView android:text="Last Name" android:textStyle="bold"/>
            <TextView android:text="Email" android:textStyle="bold"/>
            <TextView android:text="Activity" android:textStyle="bold"/>
            <TextView android:text="Age" android:textStyle="bold"/>
            <TextView android:text="Phone" android:textStyle="bold"/>
        </TableRow>
    </TableLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:contentDescription="Practice data list"/>

    <Button
        android:id="@+id/btnBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back"
        android:layout_margin="16dp"
        android:layout_gravity="start"/>

</LinearLayout>
